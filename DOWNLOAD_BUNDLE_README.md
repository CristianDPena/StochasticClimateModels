# Complete Download Bundle

The ZIP version of this review includes the frozen endpoint kernel at:

`methodology_pipeline_review/original_source/outputs/cache/conditional_cf_endpoint_kernel_production.rds`

This additional archive entry makes `original_source/` self-contained for the
project-defined source, frozen designs, transfer profiles, and required
numerical cache. The uncompressed review directory intentionally leaves the
61.3 MB cache at its original project path, as recorded in
`source_inventory.csv`.

After extracting the ZIP:

1. use `methodology_pipeline_review/original_source/` as the working directory;
2. install or use R 4.5.2;
3. follow `methodology_pipeline_review/HOW_TO_RUN.md`.

The archive does not include the R runtime or the optional validation
checkpoint directory.

